<style>
  .qb-trust-section {
    padding: 40px 16px;
    background: #fff;
    border-top: 1px solid #e8e8e8;
  }

  @media (min-width: 1024px) {
    .qb-trust-section {
      padding: 60px 48px;
    }
  }

  .qb-trust-title {
    font-size: 24px;
    font-weight: 900;
    color: #111111;
    text-align: center;
    margin-bottom: 40px;
  }

  @media (min-width: 1024px) {
    .qb-trust-title {
      font-size: 32px;
    }
  }

  .qb-trust-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }

  @media (min-width: 768px) {
    .qb-trust-grid {
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
    }
  }

  .qb-trust-card {
    background: linear-gradient(135deg, #FFF8E8 0%, #FFFBF0 100%);
    border: 1px solid #e8e8e8;
    border-radius: 16px;
    padding: 24px 16px;
    text-align: center;
    transition: all 0.3s ease;
    cursor: default;
  }

  .qb-trust-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    border-color: #F4B400;
  }

  .qb-trust-icon {
    font-size: 40px;
    margin-bottom: 12px;
    line-height: 1;
  }

  .qb-trust-name {
    font-size: 14px;
    font-weight: 700;
    color: #111111;
    margin-bottom: 8px;
  }

  .qb-trust-desc {
    font-size: 12px;
    color: #666;
    line-height: 1.4;
  }
</style>

<div class="qb-trust-section">
  <h2 class="qb-trust-title">Why Choose QuickBee?</h2>

  <div class="qb-trust-grid animate-stagger">
    <div class="qb-trust-card">
      <div class="qb-trust-icon">🚚</div>
      <div class="qb-trust-name">Fast Delivery</div>
      <div class="qb-trust-desc">Delivery within 20 minutes across town</div>
    </div>

    <div class="qb-trust-card">
      <div class="qb-trust-icon">✓</div>
      <div class="qb-trust-name">Genuine Products</div>
      <div class="qb-trust-desc">100% authentic items from trusted brands</div>
    </div>

    <div class="qb-trust-card">
      <div class="qb-trust-icon">↩️</div>
      <div class="qb-trust-name">Easy Returns</div>
      <div class="qb-trust-desc">30-day money-back guarantee on all orders</div>
    </div>

    <div class="qb-trust-card">
      <div class="qb-trust-icon">💬</div>
      <div class="qb-trust-name">24/7 Support</div>
      <div class="qb-trust-desc">Customer support available round the clock</div>
    </div>
  </div>
</div>
