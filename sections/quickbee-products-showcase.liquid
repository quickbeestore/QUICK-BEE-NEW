{% schema %}
{
  "name": "Products Showcase",
  "settings": [
    {
      "type": "text",
      "id": "section_title",
      "label": "Section Title",
      "default": "Flash Deals"
    },
    {
      "type": "text",
      "id": "section_subtitle",
      "label": "Section Subtitle",
      "default": "Limited-time offers you should not miss"
    },
    {
      "type": "select",
      "id": "product_source",
      "label": "Product Source",
      "options": [
        {
          "value": "latest",
          "label": "Latest Products"
        },
        {
          "value": "collection",
          "label": "From Collection"
        },
        {
          "value": "featured",
          "label": "Featured Products"
        }
      ],
      "default": "latest"
    },
    {
      "type": "collection",
      "id": "selected_collection",
      "label": "Select Collection (if applicable)"
    },
    {
      "type": "range",
      "id": "products_limit",
      "min": 2,
      "max": 12,
      "step": 1,
      "label": "Products to Show",
      "default": 4
    },
    {
      "type": "color",
      "id": "bg_color_start",
      "label": "Background Gradient Start",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "bg_color_end",
      "label": "Background Gradient End",
      "default": "#ffffff"
    },
    {
      "type": "checkbox",
      "id": "show_discount",
      "label": "Show Discount Badge",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Products Showcase"
    }
  ]
}
{% endschema %}

<style>
.qb-section {
  padding: 40px 16px;
  background: #0A0A0A !important;
}
@media(min-width:1024px) {
  .qb-section {
    padding: 60px 48px;
  }
}
.qb-section h2 {
  font-size: 24px;
  font-weight: 900;
  margin: 0 0 8px 0;
  color: #F0F0F0;
}
@media(min-width:1024px) {
  .qb-section h2 {
    font-size: 32px;
    margin-bottom: 12px;
  }
}
.qb-section p {
  font-size: 15px;
  color: #999;
  margin: 0 0 32px 0;
}
.qb-products {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  margin-top: 20px;
}
@media(min-width:768px) {
  .qb-products {
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }
}
@media(min-width:1024px) {
  .qb-products {
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }
}
.qb-product {
  background: #111;
  border: 1.5px solid rgba(255,255,255,0.08);
  border-radius: 16px;
  overflow: hidden;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  text-decoration: none;
  color: inherit;
  display: flex;
  flex-direction: column;
  position: relative;
}
.qb-product:hover {
  border-color: #F4B400;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4), 0 0 15px rgba(244,180,0,0.1);
  transform: translateY(-5px);
}
.qb-product-img {
  width: 100%;
  aspect-ratio: 1;
  background: #181818;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48px;
  overflow: hidden;
  position: relative;
}
.qb-product-img img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center;
  padding: 12px;
  transition: transform 0.5s ease;
}
.qb-product:hover .qb-product-img img {
  transform: scale(1.08);
}
.qb-product-info {
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  flex: 1;
}
.qb-product-title {
  font-size: 14px;
  font-weight: 700;
  color: #F0F0F0;
  line-height: 1.4;
  min-height: 40px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.qb-product-delivery {
  font-size: 11px;
  color: #4ADE80;
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: 4px;
}
.qb-product-price {
  font-size: 18px;
  font-weight: 900;
  color: #F4B400;
}
.qb-product-btn {
  background: #F4B400;
  border: none;
  padding: 12px;
  border-radius: 10px;
  font-weight: 800;
  font-size: 14px;
  color: #111;
  cursor: pointer;
  transition: all 0.3s ease;
  margin-top: 10px;
  width: 100%;
}
.qb-product-btn:hover {
  background: #FFD700;
  box-shadow: 0 4px 12px rgba(244,180,0,0.3);
}
.qb-badge {
  position: absolute;
  top: 12px;
  left: 12px;
  z-index: 2;
  background: #EF4444;
  color: #fff;
  padding: 4px 10px;
  border-radius: 6px;
  font-size: 10px;
  font-weight: 800;
  text-transform: uppercase;
}
</style>

<!-- Products Showcase -->
<section class="qb-section" style="background: linear-gradient(135deg, {{ section.settings.bg_color_start }} 0%, {{ section.settings.bg_color_end }} 100%);">
  <h2>{{ section.settings.section_title }}</h2>
  <p>{{ section.settings.section_subtitle }}</p>

  <div class="qb-products">
    {% if section.settings.product_source == 'collection' and section.settings.selected_collection %}
      {% assign products = section.settings.selected_collection.products | limit: section.settings.products_limit %}
    {% elsif section.settings.product_source == 'featured' %}
      {% assign products = shop.products | sort: "created_at" | limit: section.settings.products_limit %}
    {% else %}
      {% assign products = shop.products | sort: "created_at" | reverse | limit: section.settings.products_limit %}
    {% endif %}

    {% for product in products %}
      <a href="{{ product.url }}" class="qb-product">
        <div class="qb-product-img">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | image_url: width: 600 }}" alt="{{ product.title }}">
          {% else %}
            {{ product.title | slice: 0, 1 }}
          {% endif %}
        </div>
        <div class="qb-product-info">
          <div class="qb-product-title">{{ product.title | truncatewords: 3 }}</div>
          <div class="qb-product-delivery qb-delivery-badge" data-delivery-badge="true">⚡ 18-22 mins</div>

          <div style="display: flex; align-items: center; gap: 6px;">
            <strong class="qb-product-price">
              {% if product.price %}
                Rs {{ product.price | money_without_currency | remove: "Rs" | remove: "," }}
              {% endif %}
            </strong>
            {% if section.settings.show_discount and product.compare_at_price %}
              <s style="font-size: 12px; color: #666;">
                Rs {{ product.compare_at_price | money_without_currency | remove: "Rs" | remove: "," }}
              </s>
            {% endif %}
          </div>

          <button class="qb-product-btn"
            onclick="fetch('{{ routes.cart_add_url }}', {
              method: 'POST',
              body: new FormData(Object.assign(document.createElement('form'), {
                innerHTML: '<input name=id value={{ product.variants.first.id }}><input name=quantity value=1>'
              }))
            }).then(() => window.location.href = '{{ routes.cart_url }}')">
            Add to Cart
          </button>
        </div>
      </a>
    {% endfor %}
  </div>
</section>
